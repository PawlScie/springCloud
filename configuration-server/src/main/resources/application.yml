server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PawlScie/springCloud
          search-paths: configuration
          try-master-branch: false
